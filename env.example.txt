# 环境变量配置示例
# 使用方法：复制此文件为 .env，然后填入真实的配置值

# ==============================================================================
# LLM 配置（由 TS runtime 直接读取）
# ==============================================================================

# LLM Provider 选择（可选，默认 gemini）
# 可选值: gemini, openai, custom
# LLM_PROVIDER=gemini

# LLM 模型（可选，会覆盖 provider 特定的模型配置）
# LLM_MODEL=gemini-2.5-flash

# Gemini API 配置
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.5-flash

# OpenAI API 配置（预留）
# OPENAI_API_KEY=your_openai_api_key_here
# OPENAI_MODEL=gpt-4

# Notion 配置
NOTION_TOKEN=secret_xxx
NOTION_DATABASE_ID=xxx-xxx-xxx

# GitHub 配置
GIT_REPO_PATH=/Users/chenyulin/workspace/workdoc

# 日志配置
LOG_LEVEL=INFO

# 可选：代理配置（如果需要）
# HTTP_PROXY=http://127.0.0.1:7890
# HTTPS_PROXY=http://127.0.0.1:7890
